<div class='container'></div>
  <div class='show-body'>
    <a class='show-image'><%= image_tag @product.image.url %></a>
      <ul class="list-unstyled pl-3 border">
          <h3 class="produt-name"><%= @product.name %></h3>
          <li class="description"><%= @product.description %></li>
          <li class="price"><%= @product.price.to_s(:delimited) %>円 </li>
      </ul>
      <a><%= link_to '買い物かごに入れる', product_add_to_carts_path(@product), method: :post ,class: "btn btn-success" %></a>
  </div>
</div>